<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("includes/head") %> 
    </head>
    <body class="bg-body text-txt font-sans overflow-x-hidden">
        <%- include("includes/header") %>

        <main class=" mx-auto mt-12 px-8 text-center">
            
                <input type="text" class="rounded-l-lg mb-8 bg-txt p-1 text-body" id="search" placeholder="Search" name="search" placeholder="Search" class="bg-txt p-1 text-body">
                <button id="search-btn"class="rounded-r-lg bg-txt p-1 cursor-pointer">🔍</button>
                <!-- <input type="submit" id="submit" name="submit" value="&#x1F50D" class="bg-txt p-1 cursor-pointer"> -->
            
            <h1 class="font-monsterrat font-bold text-4xl mb-3 text-pink ">Recommended Anime</h1>
            <% if (num === 0 ) {%> 
            <p class="mb-4">There is no Anime available.</p>
            <% } else { %> 
            <p class="">Find your next favorite Anime with help of our other users!</p>
            <p class="mb-2"> We found <span class="num text-pink font-bold"><%= num %></span> anime</p>
            <p>sort by</p>
            <form action="/anime" method="GET">
                <select name="sort" id="sort" class="mb-4 bg-txt text-body p-1 px-4 text-base">
                    <option value="1" selected>Newest to Oldest</option>
                    <option value="2">Oldest to Newest</option>
                    <option value="3">Name (ascending)</option>
                    <option value="4">Name (descending)</option>
                </select>
                <button type="submit" class="bg-pink w-full block py-2 text-base font-monsterrat font-bold hover:bg-darkpink max-w-sm mx-auto  mb-12 w-52">Sort</button>
            </form>
            
            <% } %> 
            
            <ul id="anime-list">
                <% for (const anime of animeList) {%> 
                <%- include("includes/animeItem", {anime: anime}) %> 
                <% } %> 
                
                
            </ul>
            <script src="scripts/search.js"></script>
        </main>
    </body>
</html>
